= render "catalog/navs"
.container
	.row
		= render "catalog/add_recipe"
		.col-md-10
			%h2 Список покупок
			%p На этой странице Вы можете увидеть все ингредиенты, которые необходимы, чтобы приготовить всё блюда из Вашей продуктовой корзины
			- @basket.products.each do |product|
				- if product.quantity % 1 == 0
					- quantity = product.quantity.to_i
				- else
					- quantity = product.quantity
				%p= "#{product.ingridient.name} - #{quantity} #{product.measure.name}"