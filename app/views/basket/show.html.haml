= render "catalog/navs"
.container
	.row
		= render "catalog/add_recipe"
		.col-md-10
			%h2 Список покупок
			%p Для приготовления выбранных Вами блюд необходимы следующие ингредиенты:
			- @array.each do |product|
				- if product.quantity % 1 == 0
					- quantity = product.quantity.to_i
				- else
					- quantity = product.quantity
				.col-xs-2
					%p= product.ingridient.name
				.col-xs-1
					%p#count= quantity
				.col-xs-1
					%p#measure_name= product.measure.name
				.clearfix