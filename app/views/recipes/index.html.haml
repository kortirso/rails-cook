.container
    %h1 Список рецептов
    %table.table#recipes_list
        %thead
            %tr
                %th Название
                %th Фото
                %th URL
                %th Категория
                %th Нац. кухня
                %th Пользователь
                %th Проверено?
                %th
                %th
                %th
        %tbody
            - @recipes.each do |recipe|
                %tr
                    %td= recipe.name
                    %td= image_tag recipe.image, alt: "#{recipe.name}"
                    %td= recipe.path_name
                    %td= recipe.category.caption
                    %td
                        - if recipe.country
                            = recipe.country.caption
                    %td= recipe.user_id
                    %td= recipe.visible
                    %td= link_to 'Show', recipe
                    %td= link_to 'Edit', edit_recipe_path(recipe)
                    %td= link_to 'Destroy', recipe, method: :delete, data: { confirm: 'Are you sure?' }
    %br
    = link_to 'New Recipe', new_recipe_path