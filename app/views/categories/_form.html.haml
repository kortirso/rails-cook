= form_for(@category) do |f|
    .field
        = f.label :name
        %br
        = f.text_field :name
    .field
        = f.label :caption
        %br
        = f.text_field :caption
    .actions
        = f.submit